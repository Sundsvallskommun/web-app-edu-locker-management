export type User = {
  // personId: string;
  username: string;
  name: string;
  givenName: string;
  surname: string;
  schoolUnit: string;
};

export type ClientUser = {
  name: string;
  username: string;
};
